{% extends 'base.html.twig' %}

{% block title %}
	Liste des appareils
{% endblock %}

{% block body %}
	<div class="bs-docs-section" style="padding:1.5rem">
		<h3 id="tables" style="padding-bottom:1rem">
			Liste des appareils
		</h3>
		<div class="row">
			{% for response in responses %}
				<div class="col-lg-4">
					<div class="bs-component">
						<div class="card mb-3">
							<a href="{{path("responses_show", {"id" : response.id}) }}" class="btn btn-outline-info">
								<h3>
									<img src="{{ asset ('build/images/0.png') }}">
									Evaluer
									<img src="{{ asset ('build/images/2.png') }}">
								</h3>
							</a>
							<div class="card-body">
								<h4 class="card-title text-success">{{response.devices.DeviceName}}</h4>
								{% if response.ResponseValue is defined and response.ResponseValue is not null %}
									<h6 class="card-subtitle text-muted">
										<p>Note	: "{{response.ResponseValue}}"</p>
									</h6>
								{% else %}
									<h6 class="card-subtitle text-muted">
										<p>Vous n'avez pas encore donné votre avis );</p>
									</h6>
								{% endif %}

							</div>
							<svg xmlns="http://www.w3.org/2000/svg" class="d-block user-select-none" width="100%" height="200" aria-label="Placeholder: Image cap" focusable="false" role="img" preserveaspectratio="xMidYMid slice" viewbox="0 0 318 180" style="font-size:1.125rem;text-anchor:middle">
								<rect width="100%" height="100%" fill="#868e96"></rect>
								<text x="50%" y="50%" fill="#dee2e6" dy=".3em">Image cap</text>
							</svg>
						</div>
					</div>
				</div>
			{% else %}
				<p>Vous n'avez pas encore donné d'avis</p>
			{% endfor %}
		</div>
		<a href="{{ path('home') }}" class="btn btn-primary">Retour</a>
	</div>
{% endblock %}
